# Stripe Payment Link for donations.
# Create one in the Stripe dashboard:
#   Stripe → Payment Links → New → "Customer chooses price"
# Then paste the resulting URL here (e.g. https://donate.stripe.com/xxx or https://buy.stripe.com/xxx).
# If unset, the donate section is hidden.
NEXT_PUBLIC_STRIPE_DONATION_URL=
